function stringToArray(str) {
    return str.split('');
  }
  
  const inputString = "tanmay";
  const resultArray = stringToArray(inputString);
  console.log(resultArray); // Output: [ 'h', 'e', 'l', 'l', 'o' ]
  